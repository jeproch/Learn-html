const Info = () => {
  return (
    <div className="mx-4">
      <p className=" text-secondary">
        In order to start with learning HTML, we'll need a place to actually
        edit or create this so-called HTML file. The main 2 ways this can be
        done is using a <span className="text-primary">Text Editor</span> or{" "}
        <span className="text-primary">
          IDE (Integrated Development Environment)
        </span>
        .
      </p>
    </div>
  );
};

export default Info;
